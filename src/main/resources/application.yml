spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/kitapSepeti
    username: admin
    password: 1234
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      # DDL auto mode: update, validate, create, create-drop

server:
  port: 9909

logging:
  level:
    org:
      springframework:
        security: DEBUG